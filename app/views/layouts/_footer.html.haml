%footer.jumbotron.global-footer
  .row
    .col-xs-12.col-sm-4
      %ul#bottom-menu
        %li= link_to t('site'), root_path, class: 'label label-primary'
        %li= link_to t('menu.about'), page_path(:about), class: 'label label-primary'
        %li= link_to t('menu.partners'), page_path(:partners), class: 'label label-primary'
        - City.all.each do |city|
          %li= link_to %{Все события #{t("cities.#{city.permalink}.gen")} }, search_path(city: city.permalink), class: 'label label-info'
        - EventType.all.each do |event_type|
          %li= link_to event_type.name, search_path(type: event_type.permalink), class: 'label label-warning'



    #footer-vk.col-xs-12.col-sm-4.text-center
      %h4= %{Группа ВКонтакте #{t("cities.#{@city.permalink}.gen")}} if @city.present? and @city.vk_public_url.present?
      #vk_groups
    .col-xs-12.col-sm-4
      Есть у Вас появились вопросы, пишите нам на
      = link_to "info@terenkur.com", 'mailto:info@terenkur.com'