= link_to edit_event_path(event), class: 'btn btn-fab btn-fab-mini btn-info btn-sm' do
  %i.mdi-image-edit
  .ripple-wrapper
= link_to event_path(event), method: :delete, 'data-confirm' => "Are you sure?", class: 'btn btn-fab btn-fab-mini btn-danger btn-sm' do
  %i.mdi-content-clear
  .ripple-wrapper
