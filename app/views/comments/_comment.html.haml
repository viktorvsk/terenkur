.row
  .col-xs-4.text-center.col-sm-2.col-md-1
    = link_to user_path(comment.user) do
      = comment.user.name
      = image_tag(comment.user.avatar || comment.user.avatar, class: 'img-responsive')
  .col-xs-8.col-sm-10.col-md-11
    = comment.comment
%hr/

