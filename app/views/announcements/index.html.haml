- (Date.today..Date.today.end_of_week).each do |d|
  - events = @events.select{ |e| e.days.map(&:name).include? d.to_s(:db) }
  %h3= d.to_s(:db)
  .row
    - events.in_groups_of(3, false) do |group|
      - group.each do |event|
        .col-sm-4
          .well
            %p= event.name

